{
  "name": "easy-ai-forms",
  "version": "0.1.0",
  "description": "AI-powered form builder for WordPress.",
  "private": true,
  "scripts": {
    "build": "wp-scripts build",
    "start": "wp-scripts start",
    "lint:js": "wp-scripts lint-js src",
    "format": "wp-scripts format src",
    "dist": "bash bin/dist.sh"
  },
  "devDependencies": {
    "@wordpress/scripts": "^30.0.0"
  }
}
